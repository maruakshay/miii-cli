import type { FileEntry } from '../../files/ops.js';
interface Props {
    files: FileEntry[];
    query: string;
    idx: number;
}
export declare function AtPicker({ files, query, idx }: Props): import("react/jsx-runtime").JSX.Element;
export {};
